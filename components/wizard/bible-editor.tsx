'use client'
/**
 * ② BibleEditor(前端):展示抽出的 bible,表单可改 → 确认。人在环检查点。
 *
 * @see docs/DEVELOPMENT_PLAN.md · Day 3
 */

export function BibleEditor() {
  // TODO(Day3): 调 /api/extract-bible(流式)→ 表单编辑 → 确认进 ③
  return (
    <section data-step="bible">
      <h2>② 确认 Story Bible</h2>
    </section>
  )
}
