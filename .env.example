# 复制为 .env.local 后填入真实值：cp .env.example .env.local
# ⚠️ 密钥仅服务端，绝不加 NEXT_PUBLIC_ 前缀

# LLM Provider（DeepSeek，OpenAI 兼容）
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
# 模型随便填 deepseek-chat 或 deepseek-v4-pro / deepseek-v4-flash 都行——
# client 默认强制 thinking=disabled（非思考），所以 v4-pro 也会走非思考：temperature 生效、tool_choice 稳。
LLM_MODEL=deepseek-v4-pro
# 想开思考模式（更慢更贵、忽略 temperature，抽取不需要）：取消下一行注释
# LLM_THINKING=enabled